name: VibeVoice 0.5B

run-name: VibeVoice Inference Server

on:
  workflow_dispatch:
    inputs:
      duration_hours:
        description: 'Duration (max 5.5 hours)'
        required: false
        default: '5'
        type: string
      auto_restart:
        description: 'Auto-restart before timeout'
        required: false
        default: true
        type: boolean
      instances:
        description: 'Parallel instances (1-3)'
        required: false
        default: '1'
        type: choice
        options:
          - '1'
          - '2'
          - '3'

  repository_dispatch:
    types: [restart-vibevoice-inference]

permissions:
  actions: write
  contents: read

jobs:
  inference:
    uses: ./.github/workflows/reusable-inference-containerized.yml
    with:
      model_name: "vibevoice"
      model_dir: "vibevoice-inference"
      model_repo: "onnx-community/VibeVoice-0.5B-GGUF"
      model_file: "VibeVoice-0.5B-Q4_K_M.gguf"
      cache_key_prefix: "gguf-vibevoice-q4"
      tunnel_secret_name: "CLOUDFLARE_TUNNEL_TOKEN_VIBEVOICE"
      restart_event_type: "restart-vibevoice-inference"
      workflow_file: "vibevoice-inference.yml"
      duration_hours: ${{ github.event.client_payload.duration_hours || inputs.duration_hours || '5' }}
      auto_restart: ${{ github.event.client_payload.auto_restart != false && (inputs.auto_restart != false) }}
      instances: ${{ github.event.client_payload.instances || inputs.instances || '1' }}
    secrets:
      hf_token: ${{ secrets.HF_TOKEN }}
      tunnel_token: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN_VIBEVOICE }}
      workflow_pat: ${{ secrets.WORKFLOW_PAT }}
