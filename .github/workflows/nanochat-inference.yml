name: Nanochat d32

run-name: Nanochat d32 Inference Server

on:
  workflow_dispatch:
    inputs:
      duration_hours:
        description: 'How long to run (max 5.5 hours)'
        required: false
        default: '5'
        type: string
      auto_restart:
        description: 'Auto-restart before timeout'
        required: false
        default: true
        type: boolean
      instances:
        description: 'Number of parallel instances (1-3)'
        required: false
        default: '1'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      gguf_repo:
        description: 'Hugging Face repo id for GGUF (e.g., user/nanochat-gguf)'
        required: false
        type: string
      gguf_file:
        description: 'GGUF filename to download (e.g., nanochat-d32-Q4_K_M.gguf)'
        required: false
        type: string

  repository_dispatch:
    types: [restart-nanochat-inference]

jobs:
  inference:
    name: Nanochat Server #${{ matrix.instance }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 350
    permissions:
      actions: write
      contents: read
    strategy:
      matrix:
        instance: ${{ fromJson(github.event.inputs.instances == '3' && '[1,2,3]' || github.event.inputs.instances == '2' && '[1,2]' || '[1]') }}
      fail-fast: false

    steps:
      - name: Find previous running workflow
        id: find_previous
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Current run ID: ${{ github.run_id }}"

          API_RESPONSE=$(curl -s \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/nanochat-inference.yml/runs?status=in_progress")

          RUN_COUNT=$(echo "$API_RESPONSE" | jq '.total_count')
          echo "Total in_progress runs found: $RUN_COUNT"

          PREVIOUS_RUN_ID=$(echo "$API_RESPONSE" | jq -r ".workflow_runs[] | select(.id < ${{ github.run_id }}) | .id" | head -1)

          if [ -n "$PREVIOUS_RUN_ID" ] && [ "$PREVIOUS_RUN_ID" != "null" ] && [ "$PREVIOUS_RUN_ID" != "" ]; then
            echo "Found previous running workflow: $PREVIOUS_RUN_ID"
            echo "previous_run_id=$PREVIOUS_RUN_ID" >> $GITHUB_OUTPUT
          else
            echo "No previous running workflow found"
            echo "previous_run_id=" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "app/*/requirements.txt"

      - name: Set up Python and create venv
        run: |
          sudo apt-get update
          sudo apt-get install -y git
          uv venv venv
          echo "PATH=$GITHUB_WORKSPACE/venv/bin:$PATH" >> $GITHUB_ENV
          echo "VIRTUAL_ENV=$GITHUB_WORKSPACE/venv" >> $GITHUB_ENV
          python --version

      - name: Cache Hugging Face models
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/huggingface
            /tmp/hf_cache
          key: hf-nanochat-${{ runner.os }}-${{ runner.arch }}
          restore-keys: |
            hf-nanochat-${{ runner.os }}-
            hf-nanochat-

      - name: Install dependencies
        run: |
          uv pip install -r app/nanochat-inference/requirements.txt

      - name: Clone nanochat repo
        run: |
          cd app/nanochat-inference
          if [ ! -d nanochat_repo/.git ]; then
            git clone https://github.com/karpathy/nanochat.git nanochat_repo
          else
            echo "nanochat_repo already exists"
          fi

      - name: Install cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/

      - name: Start inference server
        env:
          HF_HOME: /tmp/hf_cache
          NANOCHAT_MODEL_NAME: d32
          PYTHONPATH: ${{ github.workspace }}/app/nanochat-inference/nanochat_repo
          NANOCHAT_GGUF_REPO: ${{ inputs.gguf_repo }}
          NANOCHAT_GGUF_FILE: ${{ inputs.gguf_file }}
        run: |
          cd app/nanochat-inference
          echo "=== Starting Nanochat server (Uvicorn) ==="
          uvicorn inference_server:app --host 0.0.0.0 --port 8000 > /tmp/inference_server.log 2>&1 &
          SERVER_PID=$!
          echo "Server PID: $SERVER_PID"
          echo "Waiting for server to start..."

          for i in {1..60}; do
            if ! kill -0 $SERVER_PID 2>/dev/null; then
              echo "CRITICAL: Server process $SERVER_PID has died!"
              echo "--- Last 50 lines of log ---"
              tail -n 50 /tmp/inference_server.log
              exit 1
            fi

            if curl -s http://localhost:8000/health | grep -q "ok"; then
              echo "Server is healthy!"
              break
            fi
            echo "Waiting for server... attempt $i/60"

            if [ $((i % 5)) -eq 0 ]; then
              echo "--- Server Log Tail ---"
              tail -n 10 /tmp/inference_server.log || true
              echo "-----------------------"
            fi
            sleep 5
          done

          curl -s http://localhost:8000/health || { echo "Server failed to start"; cat /tmp/inference_server.log; exit 1; }

      - name: Start Cloudflare Tunnel
        env:
          CLOUDFLARE_TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN_NANOCHAT }}
        run: |
          if [ -n "$CLOUDFLARE_TUNNEL_TOKEN" ]; then
            cloudflared tunnel --no-autoupdate run --token "$CLOUDFLARE_TUNNEL_TOKEN" 2>&1 | tee /tmp/tunnel.log &
            TUNNEL_PID=$!
            echo "Tunnel PID: $TUNNEL_PID"
            echo "Cloudflare tunnel started"
            sleep 15
            echo "=== Tunnel Log (first 30 lines) ==="
            head -30 /tmp/tunnel.log || true
          else
            echo "No tunnel token provided, server only accessible locally"
          fi

      - name: Cancel previous workflow (graceful handoff)
        if: steps.find_previous.outputs.previous_run_id != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PREVIOUS_RUN_ID: ${{ steps.find_previous.outputs.previous_run_id }}
        run: |
          echo "New server is ready, cancelling previous workflow run: $PREVIOUS_RUN_ID"
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/runs/$PREVIOUS_RUN_ID/cancel")

          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          if [ "$HTTP_CODE" = "202" ]; then
            echo "Waiting 3 minutes for graceful handoff..."
            for i in {1..18}; do
              printf "  Handoff wait: %d/180 seconds...\r" "$((i * 10))"
              sleep 10
            done
            echo "Handoff complete."
          else
            echo "Cancel request returned code $HTTP_CODE - previous workflow may have already ended"
            sleep 5
          fi

      - name: Health monitoring loop
        env:
          DURATION_HOURS: ${{ inputs.duration_hours || '5' }}
          AUTO_RESTART: ${{ inputs.auto_restart == false && 'false' || 'true' }}
          INSTANCES: ${{ inputs.instances || '1' }}
        run: |
          set -e
          cleanup() {
            echo "=== Shutting down ==="
            pkill -f cloudflared || true
            pkill -f inference_server.py || true
            exit 0
          }
          trap cleanup SIGTERM SIGINT

          DURATION_SECONDS=$((DURATION_HOURS * 3600))
          RESTART_BUFFER=360
          RESTART_THRESHOLD=$((DURATION_SECONDS - RESTART_BUFFER))

          START_TIME=$(date +%s)
          END_TIME=$((START_TIME + DURATION_SECONDS))

          echo "=== Nanochat d32 Inference Server ==="
          echo "Server running on port 8000"
          echo "Duration: ${DURATION_HOURS} hours"
          echo "Will run until: $(date -d @$END_TIME)"

          RESTART_TRIGGERED=false
          LOOP_COUNT=0
          while true; do
            CURRENT_TIME=$(date +%s)
            ELAPSED=$((CURRENT_TIME - START_TIME))
            REMAINING=$((END_TIME - CURRENT_TIME))
            LOOP_COUNT=$((LOOP_COUNT + 1))

            SERVER_OK=$(curl -sf http://localhost:8000/health > /dev/null 2>&1 && echo "OK" || echo "DOWN")
            TUNNEL_OK=$(pgrep -f cloudflared > /dev/null && echo "OK" || echo "DOWN")
            printf "[%s] Server: %s | Tunnel: %s | Elapsed: %dm | Remaining: %dm\n" \
              "$(date '+%H:%M:%S')" "$SERVER_OK" "$TUNNEL_OK" "$((ELAPSED/60))" "$((REMAINING/60))"

            if [ $((LOOP_COUNT % 5)) -eq 0 ]; then
              echo "--- Recent tunnel log ---"
              tail -5 /tmp/tunnel.log 2>/dev/null || true
              echo "--- Recent server log ---"
              tail -5 /tmp/inference_server.log 2>/dev/null || true
              echo "-------------------------"
            fi

            if [ "$SERVER_OK" = "DOWN" ]; then
              echo "Restarting server..."
              pkill -f inference_server.py || true
              cd app/nanochat-inference
              uvicorn inference_server:app --host 0.0.0.0 --port 8000 >> /tmp/inference_server.log 2>&1 &
              sleep 10
            fi

            if [ "$TUNNEL_OK" = "DOWN" ]; then
              echo "Restarting Cloudflare tunnel..."
              cloudflared tunnel --no-autoupdate run --token ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN_NANOCHAT }} 2>&1 | tee -a /tmp/tunnel.log &
              sleep 5
            fi

            if [ "$ELAPSED" -gt "$RESTART_THRESHOLD" ] && [ "$RESTART_TRIGGERED" = "false" ]; then
              if [ "${{ inputs.auto_restart != false }}" = "true" ]; then
                echo "Triggering restart workflow..."
                PAT_TOKEN="${{ secrets.WORKFLOW_PAT }}"
                if [ -n "$PAT_TOKEN" ]; then
                  RESPONSE=$(curl -sX POST \
                    -H "Accept: application/vnd.github+json" \
                    -H "Authorization: Bearer $PAT_TOKEN" \
                    -H "X-GitHub-Api-Version: 2022-11-28" \
                    https://api.github.com/repos/${{ github.repository }}/dispatches \
                    -d "{\"event_type\":\"restart-nanochat-inference\",\"client_payload\":{\"instances\":\"${{ inputs.instances || '1' }}\"}}" \
                    -w "\nHTTP_STATUS:%{http_code}" 2>&1)
                  HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
                  echo "HTTP Status: $HTTP_CODE"
                else
                  echo "WORKFLOW_PAT not set; cannot auto-restart"
                fi
                RESTART_TRIGGERED=true
              else
                RESTART_TRIGGERED=true
              fi
            fi

            if [ $CURRENT_TIME -ge $END_TIME ]; then
              echo "Duration exceeded, shutting down..."
              break
            fi

            sleep 30
          done

          cleanup

      - name: Show logs on failure
        if: failure()
        run: |
          echo "=== Inference Server Log ==="
          cat /tmp/inference_server.log 2>/dev/null || echo "No server log"
          echo ""
          echo "=== Tunnel Log ==="
          cat /tmp/tunnel.log 2>/dev/null || echo "No tunnel log"
